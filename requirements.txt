# 최소 의존성만 — torch/torchaudio/numpy는 base image에 이미 포함
# qwen_tts는 gradio/fastapi 등 UI deps 끌어오므로 Dockerfile에서 --no-deps로 설치
transformers==4.57.3
accelerate==1.12.0
einops
librosa
soundfile
soxr
scipy
kernels
pydub
sox
onnxruntime
huggingface_hub
hf_transfer
runpod>=1.7.0
